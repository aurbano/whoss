import { Options } from './constants';
/**
 * whoss
 *
 * Utility to parse npm packages used in a project and generate an attribution file to include in your product.
 *
 */
export default function whoss(options: Options): void;
